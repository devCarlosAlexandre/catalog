import 'package:flutter/material.dart';

abstract class AppColors {
  static const Color primary = Color.fromRGBO(0, 0, 0, 1);
  static const Color primaryLight = Color(0xFF1F1212);
  static const Color primaryDark = Color.fromARGB(255, 10, 6, 6);
  static const Color primaryText = Colors.white;
  static const Color hoverActive = Color(0XFFFEC82A);
}